{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "request": "launch",
            "name": "Launch Program",
            "skipFiles": [
                "<node_internals>/**"
            ],
            "program": "${workspaceFolder}/server.js", // Path to your server.js or entry point file
            "env": {
                "NODE_ENV": "development"  // Set environment variables if needed
            }
        }
    ]
}
